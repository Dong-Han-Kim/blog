# 기술 블로그 프로젝트 로드맵 v2

> MDX 기반 콘텐츠 + Supabase 익명 댓글 시스템

---

## 수정된 기술 스택

| 영역 | 기술 | 비고 |
|------|------|------|
| **프레임워크** | Next.js (App Router) + TypeScript | |
| **UI** | Tailwind CSS + shadcn/ui | |
| **콘텐츠** | MDX (로컬 파일) | ~~DB 저장~~ → 파일 기반 |
| **MDX 처리** | next-mdx-remote 또는 contentlayer2 | MDX 파싱 + React 컴포넌트 지원 |
| **코드 하이라이팅** | rehype-pretty-code + shiki | |
| **댓글 DB** | Supabase (PostgreSQL) | 댓글 데이터만 저장 |
| **폼 검증** | React Hook Form + Zod | 댓글 작성 폼 |
| **배포** | Vercel | |
| **SEO** | Next.js Metadata API | |

### 제거된 기술

| 제거 항목 | 이유 |
|-----------|------|
| ~~Supabase Auth~~ | 익명 댓글이므로 인증 불필요 |
| ~~Supabase Storage~~ | 이미지는 public 폴더 또는 외부 CDN 사용 |
| ~~미들웨어 (세션 갱신)~~ | Auth 없으므로 불필요 |
| ~~서버/브라우저 클라이언트 분리~~ | Auth 세션 관리 불필요, 단일 클라이언트로 충분 |
| ~~(auth) 라우트 그룹~~ | 로그인/회원가입 페이지 불필요 |
| ~~관리자 글 작성/수정 페이지~~ | MDX 파일로 직접 작성 |

---

## 프로젝트 폴더 구조

```
my-blog/
├── app/
│   ├── layout.tsx                  # 루트 레이아웃
│   ├── page.tsx                    # 홈 (최신 글 목록)
│   ├── posts/
│   │   ├── page.tsx                # 전체 글 목록
│   │   └── [slug]/
│   │       └── page.tsx            # 글 상세 + 댓글
│   ├── categories/
│   │   └── [category]/
│   │       └── page.tsx            # 카테고리별 글 목록
│   ├── tags/
│   │   └── [tag]/
│   │       └── page.tsx            # 태그별 글 목록
│   └── about/
│       └── page.tsx                # 소개 페이지
│
├── content/                        # ⭐ MDX 콘텐츠 폴더
│   └── posts/
│       ├── my-first-post.mdx
│       ├── nextjs-tutorial.mdx
│       └── supabase-guide.mdx
│
├── components/
│   ├── layout/
│   │   ├── Header.tsx
│   │   ├── Footer.tsx
│   │   └── Sidebar.tsx
│   ├── posts/
│   │   ├── PostCard.tsx            # 글 카드 (목록용)
│   │   ├── PostContent.tsx         # MDX 렌더링 컴포넌트
│   │   ├── TableOfContents.tsx     # 목차 자동 생성
│   │   └── CodeBlock.tsx           # 코드 하이라이팅 커스텀
│   ├── comments/
│   │   ├── CommentSection.tsx      # 댓글 영역 ("use client")
│   │   ├── CommentForm.tsx         # 댓글 작성 폼
│   │   ├── CommentList.tsx         # 댓글 목록
│   │   └── CommentItem.tsx         # 개별 댓글
│   ├── mdx/                        # MDX에서 사용할 커스텀 컴포넌트
│   │   ├── Callout.tsx             # 강조 박스
│   │   ├── CodeDemo.tsx            # 인터랙티브 코드 데모
│   │   └── ImageWithCaption.tsx    # 캡션 포함 이미지
│   └── ui/                         # shadcn/ui 컴포넌트
│
├── lib/
│   ├── mdx.ts                      # MDX 파일 읽기/파싱 유틸
│   ├── supabase/
│   │   └── client.ts               # Supabase 클라이언트 (댓글용)
│   └── validations/
│       └── comment.ts              # Zod 스키마 (댓글 폼 검증)
│
├── public/
│   └── images/                     # 블로그 이미지
│
├── styles/
│   └── globals.css
│
├── .env.local                      # Supabase URL + Anon Key
├── next.config.ts
├── tailwind.config.ts
└── tsconfig.json
```

### 기존 대비 구조 변화

```
제거됨                              추가/변경됨
──────────────────────────────      ──────────────────────────────
app/(auth)/login/                   content/posts/*.mdx
app/(auth)/register/                components/mdx/ (커스텀 컴포넌트)
app/admin/write/                    lib/mdx.ts (MDX 유틸)
app/admin/manage/
lib/supabase/server.ts              lib/supabase/client.ts (단일)
middleware.ts (세션 갱신)
```

---

## Phase 1: 환경 구축 + MDX 기초 (3~4일)

### 1-1. 프로젝트 초기화 (1일)

```
작업 내용
├── Next.js + TypeScript 프로젝트 생성
├── Tailwind CSS + shadcn/ui 설정
├── 폴더 구조 생성
├── ESLint + Prettier 설정
└── Git 초기화 + 첫 커밋
```

### 1-2. MDX 환경 설정 (1~2일)

```
작업 내용
├── MDX 처리 라이브러리 설치 및 설정
│   └── next-mdx-remote 또는 contentlayer2 선택
├── content/posts/ 폴더에 샘플 MDX 파일 작성
├── MDX frontmatter 스키마 정의
│   └── title, slug, date, category, tags, description, draft
├── lib/mdx.ts 유틸 함수 구현
│   ├── getAllPosts() — 모든 포스트 메타데이터 조회
│   ├── getPostBySlug() — 특정 포스트 조회
│   ├── getPostsByCategory() — 카테고리별 필터
│   └── getPostsByTag() — 태그별 필터
└── MDX → HTML 렌더링 파이프라인 확인
```

**MDX frontmatter 예시:**
```mdx
---
title: "Next.js App Router 완벽 가이드"
slug: "nextjs-app-router-guide"
date: "2025-03-15"
category: "Next.js"
tags: ["react", "nextjs", "app-router"]
description: "Next.js App Router의 핵심 개념을 정리합니다."
draft: false
---

# Next.js App Router 완벽 가이드

여기서부터 본문 시작...
```

### 1-3. Supabase 기초 설정 (1일)

```
작업 내용
├── Supabase 프로젝트 생성 (기존 프로젝트 활용 가능)
├── comments 테이블 생성 (SQL Editor)
├── RLS 정책 설정 (익명 INSERT 허용, DELETE는 비밀번호 검증)
├── .env.local 환경변수 설정
└── Supabase 클라이언트 생성 (lib/supabase/client.ts)
```

**comments 테이블 SQL:**
```sql
CREATE TABLE comments (
  id UUID DEFAULT gen_random_uuid() PRIMARY KEY,
  post_slug TEXT NOT NULL,              -- MDX 파일의 slug과 연결
  nickname TEXT NOT NULL,               -- 작성자 닉네임
  password_hash TEXT NOT NULL,          -- 삭제용 비밀번호 (해싱 저장)
  content TEXT NOT NULL,                -- 댓글 내용
  parent_id UUID REFERENCES comments(id) ON DELETE CASCADE, -- 대댓글용
  created_at TIMESTAMPTZ DEFAULT now() NOT NULL
);

-- 인덱스
CREATE INDEX idx_comments_post_slug ON comments(post_slug);
CREATE INDEX idx_comments_created_at ON comments(created_at);

-- RLS 활성화
ALTER TABLE comments ENABLE ROW LEVEL SECURITY;

-- 누구나 읽기 가능
CREATE POLICY "댓글 읽기" ON comments
  FOR SELECT USING (true);

-- 누구나 작성 가능
CREATE POLICY "댓글 작성" ON comments
  FOR INSERT WITH CHECK (true);

-- 삭제는 Supabase Edge Function에서 비밀번호 검증 후 처리
```

### Phase 1 완료 시 결과물
- [x] 프로젝트 기본 구조 완성
- [x] 샘플 MDX 파일이 파싱되어 데이터로 변환됨
- [x] Supabase 댓글 테이블 생성 + 연결 확인

---

## Phase 2: 읽기 기능 (5~7일)

### 2-1. 글 목록 페이지 (2~3일)

```
작업 내용
├── 홈페이지 — 최신 글 N개 표시
├── /posts — 전체 글 목록 (페이지네이션)
├── PostCard 컴포넌트 (썸네일, 제목, 요약, 날짜, 태그)
├── 글 정렬 (최신순)
└── draft: true인 글은 목록에서 제외
```

### 2-2. 글 상세 페이지 (2~3일)

```
작업 내용
├── /posts/[slug] 동적 라우팅
├── MDX 렌더링 (커스텀 컴포넌트 매핑)
├── 코드 하이라이팅 (rehype-pretty-code + shiki)
├── 목차(TOC) 자동 생성
├── 이전글/다음글 네비게이션
└── generateStaticParams()로 정적 생성
```

### 2-3. 카테고리 & 태그 (1~2일)

```
작업 내용
├── /categories/[category] — 카테고리별 글 목록
├── /tags/[tag] — 태그별 글 목록
├── 사이드바 또는 헤더에 카테고리/태그 네비게이션
└── 각 카테고리/태그별 글 개수 표시
```

### 2-4. MDX 커스텀 컴포넌트 (1일)

```
작업 내용
├── Callout 컴포넌트 (info, warning, tip 등)
├── CodeBlock 커스텀 (복사 버튼, 파일명 표시)
├── ImageWithCaption (캡션 포함 이미지)
└── 기타 필요한 컴포넌트
```

### Phase 2 완료 시 결과물
- [x] MDX 글이 정적으로 렌더링되는 블로그
- [x] 카테고리/태그 필터링 작동
- [x] 코드 하이라이팅 + 목차 작동
- [x] **이 시점에서 이미 배포 가능한 읽기 전용 블로그 완성**

---

## Phase 3: 댓글 시스템 (4~5일)

### 3-1. 댓글 목록 표시 (1일)

```
작업 내용
├── CommentSection 컴포넌트 ("use client")
├── post_slug 기준으로 댓글 조회
├── 댓글 목록 렌더링 (닉네임, 내용, 작성일)
└── 대댓글 구조 표시 (parent_id 기반 트리 구조)
```

### 3-2. 댓글 작성 (1~2일)

```
작업 내용
├── CommentForm 컴포넌트
│   ├── 입력 필드: 닉네임, 비밀번호, 댓글 내용
│   └── 대댓글일 경우 parent_id 전달
├── Zod 스키마로 클라이언트 폼 검증
│   ├── 닉네임: 2~20자
│   ├── 비밀번호: 4자 이상
│   └── 내용: 1~1000자
├── Server Action 또는 Route Handler로 저장
│   └── 비밀번호는 bcrypt 해싱 후 저장
└── 작성 후 댓글 목록 갱신 (optimistic update)
```

### 3-3. 댓글 삭제 (1일)

```
작업 내용
├── 삭제 버튼 클릭 → 비밀번호 입력 모달
├── Server Action에서 비밀번호 검증
│   └── bcrypt.compare()로 해시 비교
├── 일치 시 삭제 (대댓글도 CASCADE 삭제)
└── 불일치 시 에러 메시지
```

### 3-4. 스팸 방지 (1일)

```
작업 내용
├── 허니팟 필드 (숨겨진 입력, 봇만 채움)
├── Rate Limiting (같은 IP에서 단시간 다수 작성 방지)
│   └── Supabase Edge Function 또는 Next.js API Route에서 처리
└── 기본적인 콘텐츠 필터링 (금지어 등)
```

### Phase 3 완료 시 결과물
- [x] 익명 댓글 작성/삭제 기능 작동
- [x] 대댓글 지원
- [x] 기본 스팸 방지 적용

---

## Phase 4: 부가 기능 + SEO (4~6일)

### 4-1. 검색 기능 (1~2일)

```
작업 내용
├── 클라이언트 사이드 검색 (MDX frontmatter 기반)
│   └── 빌드 시 검색 인덱스 생성 → JSON으로 export
├── 검색 UI (검색바 + 결과 목록)
└── 선택: Fuse.js 같은 fuzzy search 라이브러리 활용
```

> DB 검색 대신 정적 검색 인덱스 사용 — MDX 파일 기반이므로 이 방식이 더 적합

### 4-2. SEO 최적화 (1~2일)

```
작업 내용
├── Metadata API (title, description, OG tags)
│   └── generateMetadata()에서 MDX frontmatter 활용
├── 동적 OG 이미지 생성 (선택)
├── sitemap.xml 자동 생성
│   └── MDX 파일 목록에서 자동 생성
├── robots.txt
└── RSS 피드 생성
```

### 4-3. UX 개선 (1~2일)

```
작업 내용
├── 다크모드 (next-themes)
├── 로딩 상태 (Suspense, skeleton UI)
├── 에러 처리 (error.tsx, not-found.tsx)
├── 반응형 디자인 점검
└── 접근성 기본 체크
```

### Phase 4 완료 시 결과물
- [x] 검색 기능 작동
- [x] SEO 메타데이터 + sitemap + RSS 완성
- [x] 다크모드 + 반응형 완성

---

## Phase 5: 배포 + 운영 (1~2일)

### 5-1. Vercel 배포 (반나절)

```
작업 내용
├── Vercel 프로젝트 연결 (GitHub 연동)
├── 환경변수 설정 (Supabase URL, Anon Key)
├── 빌드 + 배포 확인
├── 커스텀 도메인 연결 (선택)
└── SSL 자동 설정 (Vercel 기본 제공)
```

### 5-2. 운영 설정 (반나절~1일)

```
작업 내용
├── Vercel Analytics 설정 (선택)
├── 에러 모니터링 (Sentry 등 — 선택)
├── 환경변수 관리 체계 정리
├── README + 프로젝트 문서화
└── 글 작성 워크플로우 정리
    └── MDX 작성 → Git push → Vercel 자동 빌드/배포
```

### Phase 5 완료 시 결과물
- [x] 프로덕션 배포 완료
- [x] 글 작성 → 배포 파이프라인 확립

---

## 전체 일정 요약

```
Phase 1: 환경 구축 + MDX + Supabase  ███░░░░░░░  3~4일
Phase 2: 읽기 기능                   █████░░░░░  5~7일
Phase 3: 댓글 시스템                  ████░░░░░░  4~5일
Phase 4: 부가 기능 + SEO             ████░░░░░░  4~6일
Phase 5: 배포 + 운영                 █░░░░░░░░░  1~2일
──────────────────────────────────────────────────
총 예상:                             약 2.5~3.5주
```

### 기존 로드맵 대비 변화

```
기존 (v1: DB 기반)                    현재 (v2: MDX 기반)
──────────────────────────────       ──────────────────────────────
Phase 3: 인증 + 쓰기 (7~10일)        Phase 3: 댓글 시스템 (4~5일)
  - Supabase Auth 설정                  - 익명 댓글 CRUD
  - 로그인/회원가입 페이지               - 비밀번호 기반 삭제
  - 관리자 글 작성 에디터                - 스팸 방지
  - 글 관리 CRUD
  - 이미지 업로드 (Storage)
  - RLS 정책 설계

총 예상: 3.5~4.5주                    총 예상: 2.5~3.5주 (약 1주 단축)
```

---

## 학습 포인트 정리

| Phase | 핵심 학습 |
|-------|----------|
| **Phase 1** | Next.js App Router 구조, MDX 처리 파이프라인, Supabase 기초 |
| **Phase 2** | RSC 데이터 페칭, 동적 라우팅, 정적 생성(SSG), 마크다운/MDX 렌더링 |
| **Phase 3** | Supabase CRUD, RLS 정책, Zod 폼 검증, 비밀번호 해싱, Server Actions |
| **Phase 4** | 클라이언트 사이드 검색, SEO 최적화, Next.js Metadata API |
| **Phase 5** | Vercel 배포, CI/CD 파이프라인, 환경변수 관리 |

### MDX 기반에서 추가로 배우게 되는 것

- MDX 파싱/렌더링 파이프라인 이해
- rehype/remark 플러그인 생태계
- 정적 사이트 생성(SSG)의 실제 적용
- React 컴포넌트와 마크다운의 결합
- Git 기반 콘텐츠 관리 워크플로우

---

## 글 작성 워크플로우 (최종)

```
1. content/posts/에 새 .mdx 파일 생성
2. frontmatter 작성 (title, slug, date, category, tags...)
3. 본문 작성 (마크다운 + React 컴포넌트)
4. 로컬에서 확인 (next dev)
5. Git commit + push
6. Vercel이 자동으로 빌드 + 배포
7. 블로그에 새 글 반영 완료
```

---

## 진행 팁

| 팁 | 이유 |
|----|------|
| **Phase 2 완료 시 바로 배포** | 읽기 전용이라도 실제 블로그로 사용 가능 |
| **실제 기술 글을 MDX로 작성하며 진행** | 학습 내용을 바로 블로그 콘텐츠로 활용 |
| **Git 커밋 자주** | 각 기능별 히스토리 + 포트폴리오 |
| **MDX 커스텀 컴포넌트는 필요할 때 추가** | 처음부터 많이 만들 필요 없음 |
| **댓글 시스템은 단순하게 시작** | 대댓글은 나중에 추가해도 됨 |
| **막히면 단순화** | 완성 우선, 리팩토링은 나중에 |
